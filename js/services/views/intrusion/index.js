import React, { Component } from 'react';
import PropTypes from 'prop-types';

import Layout from '../../containers/layout';

export default class Intrusion extends Component {
    static propTypes = {}

    static contextTypes = {}

    static defaultProps = {}

    getStyles() {}

    render() {
        return (
            <div className="view view-intrusion">
                <Layout>
                </Layout>
            </div> 
        );
    }
}
